version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - OLLAMA_BASE_URL=http://host.docker.internal:11434
    volumes:
      - ./config:/app/config
  
  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - api
